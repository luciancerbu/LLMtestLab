Act as an expert Godot 4 game engineer specializing in 2D Verlet physics, constraint solvers, and GDScript.

Your task is to build a complete, playable "Rope Bridge Builder" in a Godot 4 project. The player places anchor pins and rope segments between them, then releases a heavy boulder to roll across the bridge toward a goal flag. Use GDScript only — no C#, no external game engines, no third-party addons beyond the Godot AI MCP plugin already enabled in the project.

Do not reuse a tutorial layout or repo you already know. Implement from the rules below.

Goal:
A satisfying build-then-test physics toy where bridges sway, snap, or succeed under load. Prioritize correct Verlet integration, distance constraints, pin constraints, and stable boulder interaction first — that is what this test measures. The moment the boulder rolls (or plummets) across the bridge should be the visual payoff. Give it a clean workshop aesthetic with readable rope rendering and a dramatic slow-motion option.

### Environment & MCP Workflow (required):
You are connected to a live Godot editor via the Godot AI MCP server. You MUST use MCP tools to build, run, and verify the game — do not only output code in chat.

1. Use MCP tools to create scenes, scripts, Input Map actions, and UI directly in the editor.
2. After meaningful changes, run with `project_run` and inspect via `editor_screenshot` and `logs_read(source="editor", include_details=true)`.
3. Use `game_manage` input tools to smoke-test placing pins and releasing the boulder when possible.
4. Iterate until build mode, test mode, and win/lose states all work without errors.

### Visual & UI Requirements:
1. **Viewport:** 1280×720 world space. Side-view 2D (gravity pulls down). Dark workshop background `#1a1a2e`, subtle grid lines every 64 px.
2. **Fixed landmarks (always visible):**
   - Left cliff platform at x ≈ 120 (solid `#4a4e69`)
   - Right cliff platform at x ≈ 1160 with **Goal Flag** (`#55ff33`) — win zone
   - Gap between cliffs: ~920 px wide, ~400 px deep void below (`#0d0d14`)
3. **Rope:** Render as a polyline or chained `Line2D` / small circles per particle, color `#e9c46a`, thickness 3 px. Under tension (stretch > 5%), tint `#ff6b35`.
4. **Pins:** Small metallic circles `#adb5bd` where player placed anchors (max 6 pins total per bridge).
5. **Boulder:** `RigidBody2D` circle, radius 28 px, fill `#6c757d`, outline `#dee2e6`. Should visibly deform the bridge when rolling.
6. **HUD (`CanvasLayer`):**
   - Mode label: `BUILD` or `TEST`
   - Pin count: `Pins: 2/6`
   - Buttons: **Clear Bridge**, **Release Boulder** (enabled only in TEST), **Reset Level**
   - Toggle: **Slow-Mo** (0.25× time scale during TEST)
   - Result banner: "Bridge Held!" / "The boulder fell!" with restart hint

### Game Phases:

**BUILD phase (default on load / after Reset):**
- Click (or tap) in the gap to place a **pin** (fixed anchor constraint). Pins must be:
  - Inside the gap bounds (not on cliff solids)
  - At least 80 px apart from any other pin
  - Maximum **6 pins** per attempt
- After placing 2+ pins, click pin A then pin B to spawn a **rope** between them:
  - Rope = chain of **12 Verlet particles** connected by distance constraints
  - End particles are pinned to the two anchor positions each frame
- Right-click a pin to remove it (and any ropes attached). Cannot remove cliff anchors (optional: provide 2 pre-placed cliff-edge pins that are always fixed).
- **Release Boulder** button switches to TEST phase (or press Enter).

**TEST phase:**
- Spawn boulder on left cliff at `(160, cliff_top - 40)`.
- Apply initial horizontal velocity `vx = 180` px/s toward the gap (or let gravity + bridge slope do the work — document choice).
- Physics runs until: boulder center enters goal zone (win), boulder falls below y = 750 (lose), or 15 seconds elapse (lose).
- During TEST, pins and rope structure are frozen (no editing). Boulder collides with rope particles via physics layers or manual collision response.

### Verlet Physics (core implementation — no built-in soft-body plugin):

Each rope particle `i` has `pos`, `prev_pos`, and optional `pinned: bool`.

1. **Integration (per sub-step):**
   ```
   velocity = pos - prev_pos
   prev_pos = pos
   pos += velocity + gravity * dt * dt
   ```
   Gravity: `(0, 980)` px/s². Use **8 sub-steps** per frame (`dt / 8`).

2. **Distance constraints** between consecutive particles on the same rope:
   - Rest length = total rope length / (particle_count - 1)
   - Relax each constraint **5 iterations** per sub-step using symmetric correction (move both particles unless pinned).

3. **Pin constraints:** Pinned particles have `pos` and `prev_pos` locked to anchor each sub-step.

4. **Cliff collision:** Particles and boulder cannot penetrate cliff `StaticBody2D` rectangles. Use Godot physics for boulder-cliff; rope particles may use simple AABB clamp against cliff edges.

5. **Boulder ↔ rope interaction (required):**
   - Each frame, for each rope particle within boulder radius + margin: push particle outward along normal from boulder center, and apply equal-and-opposite impulse to boulder velocity (approximate point collision).
   - Without this, the boulder will ghost through ropes — that is a failure.

### Critical Implementation Rules (coding traps):
- **Sub-stepping is mandatory.** Single-step Verlet with large dt will explode constraints.
- **Constraint iteration count matters.** Fewer than 3 iterations produces rubber-band bridges; document your iteration count.
- **Do not use `RigidBody2D` joints for the rope itself** — the rope must be custom Verlet + constraints (joints optional only for boulder if needed).
- **Anti-tunneling:** If boulder speed × dt > rope particle spacing, do 2 boulder collision passes per sub-step.
- **BUILD/TEST state machine** must fully reset particle velocities when entering TEST (zero initial rope wobble except gravity).

### Controls:
| Input | Action |
| Left click (BUILD) | Place pin / connect two pins with rope |
| Right click (BUILD) | Remove pin |
| Enter / Release button | Start TEST |
| R | Reset to BUILD |
| S | Toggle slow-mo (TEST only) |

### Scene Structure (minimum):
- `main.tscn` — cliffs, camera, HUD, spawners
- Scripts: `verlet_rope.gd`, `bridge_builder.gd`, `boulder.gd`, `game_state.gd` (or equivalent separation)
- Main Scene set in Project Settings

### Deliverable:
A runnable Godot 4 project where the player builds a rope bridge, releases a boulder, and either reaches the goal or falls into the void. All Verlet and constraint logic implemented — no empty functions or placeholder stubs.

Before finishing, use MCP to verify and confirm:
- BUILD: place pins, connect ropes, clear, pin limit enforced
- TEST: boulder spawns, interacts with rope particles, does not ghost through
- Win when boulder reaches goal zone; lose on fall or timeout
- Slow-mo toggle works; Reset returns to BUILD cleanly
- `project_run` succeeds; `editor_screenshot` shows bridge + boulder in action
- No placeholder logic in any script
